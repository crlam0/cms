<?xml version="1.0"?>
<ruleset name="Coding standard">
    <description>Coding standard</description>

    <!-- display progress -->
    <arg value="p"/>
    <arg name="colors"/>

    <!-- inherit rules from: -->
    <rule ref="PSR2"/>
    <rule ref="Generic.Arrays.DisallowLongArraySyntax"/>
    <rule ref="Squiz.WhiteSpace.SuperfluousWhitespace">
        <properties>
            <property name="ignoreBlankLines" value="false"/>
        </properties>
    </rule>

    <!-- Paths to check -->
    <file>admin</file>
    <file>include</file>
    <file>migrations</file>
    <file>modules</file>
    <file>tests</file>

    <exclude-pattern>include/*(css|js)$</exclude-pattern>
    <exclude-pattern>include/ckeditor/*$</exclude-pattern>
    <exclude-pattern>include/edit_area/*$</exclude-pattern>
    <exclude-pattern>include/filemanager/*$</exclude-pattern>
    <exclude-pattern>modules/media/player/*$</exclude-pattern>

</ruleset>
